# Domain for the MCP server (Caddy will auto-provision TLS)
DOMAIN=obsidian.hanzpo.com

# Absolute path to your Obsidian vault on the host
VAULT_PATH=/path/to/your/obsidian/vault

# API key for authentication (generate with: ./keygen.sh)
API_KEY=
